import { Router } from "express";

const routes = Router()

export const subjectRoutes = () => {

    routes.post('/', )
    routes.get('/:subject_id')
    routes.post('/registration/:subject_id', )
   
    return routes
}